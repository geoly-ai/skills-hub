# API 参考图源说明

API 参考文档以文字契约与表格为主，图不是必备元素（design.md §3.5 的图质检规则同样适用：如果用图，仍要有图号、被引用、等效字号 ≥ 7pt）。

| 文件 | 类型 | 用途 |
|---|---|---|
| auth-flow.mmd | Mermaid sequence | 第 2 节「鉴权」的可选补充图，鉴权机制复杂（如需要刷新令牌、多步授权）时使用；简单的 Bearer Token 场景可以只用文字说明，不必配图 |
