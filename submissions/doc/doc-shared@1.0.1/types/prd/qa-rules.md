# 质检规则（PRD）

对齐 design.md §5.2 与 §3.7（引擎通用规则另见 doc-shared，不在此重复列出 T1–T12 / L1 / L2 / R1 / C1）。「自动」列为是的由 qa_rules.py 或引擎规则检查（见各行，W3-A 已实现）；否的由主代理人工检查。

| 编号 | 规则 | 判据 | 定级 | 自动/人工 |
|---|---|---|---|---|
| PRD-01 | 必备章节齐全 | skeleton.md 中 required=是 的 6 节都能匹配到标题或锚点（文档控制与修订记录由引擎按 doc.json 生成、摘要由 summary 块生成，不计入正文必备章节） | 必改 | 自动（引擎 S1） |
| PRD-02 | 每条需求有 REQ 编号且唯一、连续 | 正则匹配 REQ-[A-Z]+-\d{2}；模块内序号连续，不跳号 | 提示（试运行；原定 必改） | 自动（qa_rules.py 查格式与节内有定义；重复由引擎 S3 必改，跳号由 S3 contiguous 提示） |
| PRD-03 | 每条需求有验收标准列且非空 | 需求表格 / 小节含"验收标准"字段且非空 | 提示（试运行；原定 必改） | 自动（qa_rules.py） |
| PRD-04 | 「范围与非目标」节非空 | 该节字数 > 0 且含"非目标"或等价小标题 | 提示（试运行；原定 必改） | 自动（qa_rules.py） |
| PRD-05 | 开放问题表每行有 owner 与截止日期 | 依赖 / 风险 / 开放问题表格含 owner、截止时间两列且逐行非空 | 提示（试运行；原定 必改） | 自动（qa_rules.py） |
| PRD-06 | 指标行有口径与来源列 | 目标与指标表格含"口径"“来源”两列且非空，或段落中显式出现两者 | 提示（试运行；原定 必改） | 自动（qa_rules.py） |
| PRD-07 | 验收列无空泛词（T11 的 PRD 专属扩展） | 验收标准、异常与边界文本不含"体验更好""性能高""合理""友好" | 提示（试运行；原定 建议） | 自动（qa_rules.py 查「异常与边界」类列；验收类列由引擎 T11 查） |
| PRD-08 | 至少一张用户流程图，每张图有图号且被正文引用 | figures 目录存在 {#fig: 锚点且被 @fig: 引用至少一次 | 提示（试运行；原定 必改） | 自动（qa_rules.py 查带锚点的流程图；未被引用由引擎 X2 提示） |
| PRD-09 | 需求描述可观察行为，不暗藏实现方案 | 需求正文不出现具体技术选型词（Redis、Kafka、MySQL 等，关键词表见 glossary.json 的 forbidden_impl_terms） | 提示 | 自动（引擎 R1，pack.json banned_terms 全文匹配；误报率较高，人工复核） |
| PRD-H1 | 验收标准可测，与需求一一对应 | — | — | 人工 |
| PRD-H2 | 用户问题有证据，不是内部偏好 | — | — | 人工 |
| PRD-H3 | 优先级分布合理（不是全 P0） | — | — | 人工 |
| PRD-H4 | 异常路径覆盖了关键失败场景 | — | — | 人工 |
| PRD-H5 | 范围与非目标没有互相矛盾 | — | — | 人工 |
| PRD-H6 | 质量约束不是机械填模板 | — | — | 人工 |

## 人工检查清单（评审时逐条勾）

- [ ] summary 块独立可读，回答了问题、目标、范围、上线判据四项
- [ ] 背景节的每个用户问题陈述都能找到证据来源或标注待补证据
- [ ] 目标指标口径写清楚分子分母，未知值有 owner 与截止时间
- [ ] 场景覆盖了功能需求里引用到的所有前置情境
- [ ] 每条 P0 / P1 需求都能对应到至少一个用户场景
- [ ] 非功能需求逐项要么有门槛要么写不适用理由
- [ ] 术语表与正文用词一致，未出现术语表未收录的同义异写
- [ ] doc.json revision_history 反映的改动与本版实际差异一致

## 伪问题（不要报）

- 里程碑节缺失（按需章节，未排期的早期 PRD 可以不写）。
- 数据与埋点节缺失（按需章节，无埋点需求的功能可以不写）。
- 非功能需求某一类别写"不适用"且给出理由，不算漏项。

## 实现说明（W3-A，2026-09-15）

- 自动规则由同目录 qa_rules.py 实现，新规则一律定级「提示」试运行（doc-qa/SKILL.md「类型包接入」），「原定」为本表原定级；样张与真实文档校准后用 pack.json qa.severity_overrides 升级。校准结果见 ~/workspace/docs/doc-skill-platform-2026-09-15/wave2/qa-calibration.md §6。
- 与引擎重复的判据不在 qa_rules.py 重复实现，「自动/人工」列写明由哪条引擎规则执行（S1 必备章节、S3 编号重复与跳号、L8 代码块语言、R1 绝对化用语、T11 验收列空泛词、X2 图表未引用）。
- 跨文档规则用 ctx.related(type, role=…)：按 role 精确命中且可读才用；读不出、或同类型未写 role 的条目多于 1 条时按关联文档缺失处理（pack.json related 的 for_gate 为真时必改）；同类型未写 role 的条目恰好 1 条时兼容读取并提示补 role（迁移期回退，契约定 role 必填后删除）。
- 数据块（CSV）各行共用指令行号，逐行问题报全文级（行号 0），摘录写数据文件与 CSV 行号，避免被「同规则同行只留一条」合并。
- 关键数字（X4）与高亮限额（H1–H3）口径在 pack.json qa.key_figures、qa.highlight_limits 声明；key_figures 定级一律提示试运行，X4 只查多处取值是否一致，不查数字是否存在或正确。
