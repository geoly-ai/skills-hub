# 质检规则（测试计划）

对齐 design.md §5.5。引擎通用规则（T1–T12、L1/L2、R1、C1）不在此重复列出。

| 编号 | 规则 | 判据 | 定级 | 自动/人工 |
|---|---|---|---|---|
| PLAN-01 | 必备章节齐全 | skeleton.md 中 required=是 的 8 节都能匹配到标题或锚点（文档控制由引擎按 doc.json 生成，不计入正文必备章节） | 必改 | 自动（引擎 S1） |
| PLAN-02 | 关联需求编号在 PRD 中存在 | 需求覆盖矩阵引用的 REQ 编号需能在关联 PRD 的 data/requirements.csv（或正文 REQ 表）中找到 | 提示（试运行；原定 必改） | 自动（qa_rules.py，ctx.related 跨读 PRD） |
| PLAN-03 | PRD 每条 P0/P1 需求至少一条用例（D2 门） | 覆盖矩阵中 PRD 标记 P0/P1 的 REQ，在测试用例包关联需求里至少出现一次 | 提示（试运行；原定 必改（D2 门条件）） | 自动（qa_rules.py，ctx.related 跨读 PRD 与测试用例重算，不再只读 coverage.csv） |
| PLAN-04 | 准入准出标准含数字 | 「准入与准出标准」节正文出现至少一个数字（百分比或计数） | 提示（试运行；原定 必改） | 自动（qa_rules.py） |
| PLAN-05 | 不测范围条目均有理由 | 「测试范围与不测范围」节的不测范围列表每条紧跟"原因"或等价说明 | 提示（试运行；原定 建议） | 自动（qa_rules.py） |
| PLAN-06 | 缺陷级别有判定标准 | 「缺陷管理」节表格含级别与判定标准两列 | 提示（试运行；原定 建议） | 自动（qa_rules.py） |
| PLAN-H1 | 用例覆盖边界值、异常、并发这类高风险点 | — | — | 人工 |
| PLAN-H2 | 优先级分布合理 | — | — | 人工 |
| PLAN-H3 | 测试策略与需求风险等级匹配 | — | — | 人工 |

## 人工检查清单

- [ ] 被测版本具体到可核实的版本号或代码提交
- [ ] 环境清单不使用生产真实数据（隐私风险）
- [ ] 准出标准与测试报告的准出结论判据一致（跨文档核对）
- [ ] 需求覆盖矩阵中覆盖为 0 的需求有标注原因，不是静默缺失

## 伪问题（不要报）

- 覆盖矩阵中 P2/P3 需求无对应用例（D2 门只要求 P0/P1 覆盖，P2/P3 覆盖率低不算必改，但建议标注计划）。

## 实现说明（W3-A，2026-09-15）

- 自动规则由同目录 qa_rules.py 实现，新规则一律定级「提示」试运行（doc-qa/SKILL.md「类型包接入」），「原定」为本表原定级；样张与真实文档校准后用 pack.json qa.severity_overrides 升级。校准结果见 ~/workspace/docs/doc-skill-platform-2026-09-15/wave2/qa-calibration.md §6。
- 与引擎重复的判据不在 qa_rules.py 重复实现，「自动/人工」列写明由哪条引擎规则执行（S1 必备章节、S3 编号重复与跳号、L8 代码块语言、R1 绝对化用语、T11 验收列空泛词、X2 图表未引用）。
- 跨文档规则用 ctx.related(type, role=…)：按 role 精确命中且可读才用；读不出、或同类型未写 role 的条目多于 1 条时按关联文档缺失处理（pack.json related 的 for_gate 为真时必改）；同类型未写 role 的条目恰好 1 条时兼容读取并提示补 role（迁移期回退，契约定 role 必填后删除）。
- 数据块（CSV）各行共用指令行号，逐行问题报全文级（行号 0），摘录写数据文件与 CSV 行号，避免被「同规则同行只留一条」合并。
- 关键数字（X4）与高亮限额（H1–H3）口径在 pack.json qa.key_figures、qa.highlight_limits 声明；key_figures 定级一律提示试运行，X4 只查多处取值是否一致，不查数字是否存在或正确。
