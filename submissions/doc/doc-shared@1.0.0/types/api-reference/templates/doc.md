# {服务名称} API 参考

<!-- summary -->
本文档是 {服务名称} 的完整接口契约：{一句话服务边界}。当前版本：{v1}。鉴权方式：{方式}。
<!-- /summary -->

## 1. 接口总览

Base URL：测试环境 {https://test.example.com}；生产环境 {https://api.example.com}。

服务边界：{负责什么，不负责什么}。

<!-- table: 接口清单 -->
| 方法 | 路径 | 简述 | 状态 |
|---|---|---|---|
| {POST} | {/v1/xxx} | {一句话简述} | {稳定 / Beta / 已弃用} |

## 2. 鉴权

{鉴权方式：Bearer Token / HMAC 签名等，具体到放在请求的哪个位置。凭证获取流程。权限范围（scope）定义。}

```http
Authorization: Bearer {access_token}
```

## 3. 通用约定

### 3.1 分页

{分页参数与响应结构统一说明，接口详情节不重复。}

### 3.2 幂等

{哪些接口支持幂等、幂等键怎么传。}

### 3.3 错误码表

<!-- table: 全局错误码 -->
| 错误码 | 含义 | 可操作恢复建议 |
|---|---|---|
| {UNAUTHORIZED} | {鉴权失败} | {检查 Token 是否过期} |
| {RATE_LIMITED} | {超出限流} | {按退避策略重试} |

### 3.4 版本策略

{版本号出现位置（URL / Header）；升级与兼容策略。}

## 4. 接口详情

### 4.1 {接口名称}

{POST} {/v1/xxx}

<!-- table: 请求参数 -->
| 参数 | 类型 | 必填 | 取值范围 | 说明 |
|---|---|---|---|---|
| {字段} | {类型} | {是/否} | {范围} | {说明} |

权限：{要求的角色/scope}。行为与副作用：{是否触发通知、扣减库存等连带效果，如无显式说明"无显式副作用"}。

```json
{
  "示例请求字段": "value"
}
```

```json
{
  "示例响应字段": "value"
}
```

<!-- table: 已知错误 -->
| 错误码 | 含义 | 恢复建议 |
|---|---|---|
| {业务错误码} | {含义} | {可操作建议} |

## 5. 限流与重试

<!-- table: 限流规则 -->
| 维度 | 规则 |
|---|---|
| {IP / 账号} | {次数/时间窗} |

重试建议：{哪些错误码可重试、退避策略}。

## 6. 兼容与弃用

<!-- table: 弃用接口 -->
| 接口 | 弃用原因 | 替代方案 | 下线时间 |
|---|---|---|---|
| {旧接口} | {原因} | {新接口} | {日期} |

## 7. 变更记录

<!-- table: 变更记录 -->
| 版本 | 日期 | 变更内容 | 破坏性 |
|---|---|---|---|
| v{版本} | {日期} | {内容} | {是/否} |
