# 质检规则（测试报告）

对齐 design.md §5.7 与用户补全的六要素。引擎通用规则（T1–T12、L1/L2、R1、C1）不在此重复列出。

| 编号 | 规则 | 判据 | 定级 | 自动/人工 |
|---|---|---|---|---|
| REPORT-01 | 必备章节齐全 | skeleton.md 中 required=是 的 6 节都能匹配到标题或锚点（文档控制由引擎按 doc.json 生成，不计入正文必备章节） | 必改 | 自动（引擎 S1） |
| REPORT-02 | 准出结论取值合法 | 「准出结论与依据」节出现"建议发布""不建议发布""有条件发布"三者之一 | 提示（试运行；原定 必改） | 自动（qa_rules.py） |
| REPORT-03 | 用例执行统计按模块与优先级双维度给出 | 「用例执行统计」节含至少两张表格或一张同时含模块列与优先级列的表格 | 提示（试运行；原定 必改） | 自动（qa_rules.py） |
| REPORT-04 | 缺陷统计含级别、状态、趋势三个维度 | 「缺陷统计」节含级别分布表、状态分布表，且出现按时间的趋势描述或图表 | 提示（试运行；原定 必改） | 自动（qa_rules.py） |
| REPORT-05 | 引用的 TC 编号在关联测试用例中存在 | 正文与执行统计引用的 TC 编号能在关联 test-cases 的 data/cases.csv 中找到 | 提示（试运行；原定 必改） | 自动（qa_rules.py，ctx.related 跨读测试用例） |
| REPORT-06 | 准出依据与关联测试计划的准出标准口径一致 | 「准出结论与依据」节「标准」列（无此列时取全节）抽取「Pn 用例通过率 / 缺陷数 比较符 数值」与「无未解决 Pn 缺陷」，与关联 test-plan「准入与准出标准」节准出段落逐条比对：计划有而本文没列、比较符或数值不同、本文列了计划没有的，都报 | 提示（试运行；原定 必改） | 自动（qa_rules.py，ctx.related 跨读测试计划，数值门槛与「无未解决 Pn 缺陷」逐条比对） |
| REPORT-07 | 未通过用例与遗留问题逐条有原因与计划 | 「未通过与遗留问题」节表格含"原因""计划"两列且非空 | 提示（试运行；原定 必改） | 自动（qa_rules.py） |
| REPORT-08 | 执行率有具体数字 | 「执行概况」节出现执行率百分比或"已执行/计划总数"的具体计数 | 提示（试运行；原定 必改） | 自动（qa_rules.py） |
| REPORT-H1 | 未通过原因是否真实、不是"待排查"这类占位 | — | — | 人工 |
| REPORT-H2 | 遗留问题若决定不修复上线，是否有明确的批准人与风险评估 | — | — | 人工 |
| REPORT-H3 | 风险与建议是否可执行、与 PRD 目标指标或 tech-spec 可观测性呼应 | — | — | 人工 |

## 人工检查清单

- [ ] 报告日期是实际生成日期，不是计划日期
- [ ] 缺陷发现趋势能反映质量是否收敛（后期新增缺陷数下降），不只是罗列总数
- [ ] 准出结论的三选一与实际数据支撑的结论方向一致（不能数据不达标却写"建议发布"而不说明条件）
- [ ] 附录给出的明细数据路径可实际打开核对

## 伪问题（不要报）

- 附录节缺失（按需章节，正文统计表已完整时可以不单独列附录，但要在正文注明明细数据的引用位置）。
- 缺陷统计中某级别数量为 0（说明该级别未发现缺陷，是正常结果，不是漏项）。

## 实现说明（W3-A，2026-09-15）

- 自动规则由同目录 qa_rules.py 实现，新规则一律定级「提示」试运行（doc-qa/SKILL.md「类型包接入」），「原定」为本表原定级；样张与真实文档校准后用 pack.json qa.severity_overrides 升级。校准结果见 ~/workspace/docs/doc-skill-platform-2026-09-15/wave2/qa-calibration.md §6。
- 与引擎重复的判据不在 qa_rules.py 重复实现，「自动/人工」列写明由哪条引擎规则执行（S1 必备章节、S3 编号重复与跳号、L8 代码块语言、R1 绝对化用语、T11 验收列空泛词、X2 图表未引用）。
- 跨文档规则用 ctx.related(type, role=…)：按 role 精确命中且可读才用；读不出、或同类型未写 role 的条目多于 1 条时按关联文档缺失处理（pack.json related 的 for_gate 为真时必改）；同类型未写 role 的条目恰好 1 条时兼容读取并提示补 role（迁移期回退，契约定 role 必填后删除）。
- 数据块（CSV）各行共用指令行号，逐行问题报全文级（行号 0），摘录写数据文件与 CSV 行号，避免被「同规则同行只留一条」合并。
- 关键数字（X4）与高亮限额（H1–H3）口径在 pack.json qa.key_figures、qa.highlight_limits 声明；key_figures 定级一律提示试运行，X4 只查多处取值是否一致，不查数字是否存在或正确。
