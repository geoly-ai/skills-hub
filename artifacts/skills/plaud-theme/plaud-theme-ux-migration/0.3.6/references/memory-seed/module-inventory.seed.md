# 模块清单（per-module metadata）

> ⚠️ **一次性种子 — 2026-07 快照。** 首次使用时复制到项目 `memory/模块清单.md`，之后即由项目维护，**不再随包更新**。
> 字段含义见 `../project-state-schema.md`；使用规则见 `README.md`。
>
> 🔴 **本文件内的字号 / 间距 / 颜色 / 断点等数值是「实测历史证据」（2026-07 当时仓库的实际状态），不是规范值。规范一律以 `plaud-theme-shared/references/` 为准；两者不一致时以 shared 为准，禁止拿本文件的数值当 spec 落地或反推覆盖 shared。**

> 每完成一个模块的迁移加一行。启动新模块迁移前先看这张表。「实例数」以 `AssessmentRef` 为准，变了要回写。

| 模块 (代码标识符) | Shopify 后台名 | 实例数 | 迁移状态 | 已知 schema 约束 | 关键 admin 字段 / 备注 |
| --- | --- | --- | --- | --- | --- |
| sa-text | SA: Text | 3 | ✅ v1.3 wave 已加 scheme opt-in + remove_duplicate_spaces | — | bg_color / text_color_v2 (v2 强制 spec #000)；自定义元素 `<sa-text>` 自带 block，scheme 类直接挂在它上 |
| slideshow | Slideshow | 38 (37+ 模板) | 🟡 配置层迁移中（index / notepin-s-product 已切 New Slide）| heading/subheading/des 字号都 step:1 自由 | **配置层迁移**：启用 `new_slide`（spec 字号类）、隐藏旧 `slider_item`，详见 §4.8。逐模板翻 `disabled`；New Slide 文字块定位用 左%/宽%/垂直锚点 系统、动效 fade-in-down。**无预置 new_slide 的模板需手动新建（§4.8.1），且必须开 Enable New Banner（§4.8.2），否则高度脱锚、100px 安全区挤压内容** |
| ss-intelligent-power | Intelligent Power SS | 3 | ✅ v1.3 wave 已加 scheme + disclaimer + align config | — | 内嵌 `<style>` 切换断点；plaud-team 用 PC+MB 双图，其它仅 PC |
| delta-accordion | Delta Accordion | 32 | ✅ v1.3 wave 已加 scheme + disclaimer + 进度条 / 左侧线色 token 化 | — | 颜色已用 `var(--color-label-tertiary)` (#A3A3A3) 统一；calcMinHeight 已删受 CLS 抖动 |
| sa-team-dtc-editorial-split | SA Team DTC Editorial | 少量 | ✅ v1.3 wave 已加 scheme + disclaimer + 删 4 个 padding 字段 + 删按钮颜色 lock | — | bg-white 与 scheme 互斥；CTA 不再硬锁 brand-dark，跟随默认方案 |
| sa-team-dtc-landing | SA: Team DTC Landing | 1 | ✅ v1.3 wave 已加 heading 配置 + disclaimer + 删 4 个 padding 字段 + 删按钮颜色 lock；**不接 scheme**（卡片身份） | — | Step 2 价格 amount 28/24 (title-2)；Toggle 14/12；CTA 用 Primary-MD；Launch Offer 嵌套是历史遗留待评估 |
| tt-faq-list | Faq List TT | 57 | ✅ pattern 起点；v1.3 wave verify pass；2026-06-15 figma 二次对齐（分隔线移到 item 边界 + 首条上线、问题↔加减号 24、图标 20、展开间距用 .active、PC/MB 间距统一） | — | 问题 fs-headline / 答案 fs-body-lg；富文本 H 标签字号未对齐 spec 是全局议题；plaud-team-page 移动端区头按 figma 改居中 |
| tt-faq | Faq TT | 少量 | ✅ v1.3 wave 已加（参照 Faq List TT） | — | 跟 Faq List TT 共享 tt-faq-item snippet |
| ww-multi-content | Multi Content - WW | 91 | ✅ schema 放宽 + plaud-team 4 实例；v1.3 wave 修 disclaimer（去 mt-20 / fs-12、加 Mobile 对齐、`right→end` 映射）；index「Plaud Intelligence」配置层（卡片标题→headline、Learn More→Medium）；**「Made for real work」可展开卡片 2026-06-11 代码层全 spec 对齐**（字号/间距/圆角/按钮 + 展开标签居中、hover 仅 PC 两处修复）；**2026-06-22 区头 `.section__header` 去 `mb-33/mb-sm-20`，间距交回 cs-section-header space-8（33→32px 全站）**；**2026-06-23 取消 `{% if column_gap > 30 %}row-gap-30{% else %}row-gap{% endif %}` 的 30px 锁，恒用 `row-gap`（移动端竖向堆叠跟随 column_gap_mb；仅 3 个 section column_gap>30 实例受影响：0426pins yNzzEn + plaud-team ×2，PC 单行不变、移动端 30→16）**；**2026-06-23 product.note-0911 6 实例迁移**（数据价值区按 0426pins 重建但保留 PC 左图右文 / 规格表 20/18+gap24/16 / 对比图+banner / 移动端区头左对齐）+ 两处模块级：① **叠图 banner（`image_with_text_overlay`）标题/描述去流式 clamp、恒用 `fs-custom` 按后台字号离散渲染**（约 16 个启用实例回归，§4.12.1）② **底部按钮 `margin-top:48px→.mar-t-8`(32) + 叠加 `.btn-primary-md` + schema 默认背景色 #333333→#413D3B**（3 个启用实例 note-0911/shop-plaud-2/plaud-team-tour + 新建默认）；**2026-06-23 product.notepin-0915 5 实例迁移**（纯本页存值、无模块改动：区头移动端左 / 安全提示+脚注移入内置 Disclaimer / 「Why choose」说明 body-lg 16/16 + 列距 24/16 / 规格表 20/18 + gap24/16；纯图 LUVPaV 跳过）；**2026-06-23 product.plaud-note-pro 4 启用实例**（数据价值区 AUz7nA 为 2 列简单结构、PC 左文右图、按 Figma 拆分列做 M 端重排「标题→图→数据→脚注」；规格表 / ELBerj 区头左；纯图 azEQGy 跳过；3 disabled 不计）| column_gap step 5→1、max 50→60、default 30→24 / 15→16 | 模块自带 admin 配色方案，**不接** enable_color_scheme 两段式 opt-in；可展开卡片(oura)字号/色走 critical 工具类挂 HTML（避 FOUC），白字压图不套 spec 灰阶；**作为旧版 Multi Content 的 1:1 重建目标模块——结构 = col_parent→子块、image block 懒加载有 bug，详见 §4.12** |
| main-product | （产品详情 / Product information）| 36 | 🟡 进行中（**吸底加购栏 Type A 已迁 + 抽成共享片段 `snippets/product-sticky-bar.liquid`，仅接 style-1/audience1**；数量选择器/加购按钮/加减图标为共享组件、本轮全站对齐 Figma）| — | **吸底栏两套实现**：Type A（style-1/2，div+总价块+buy-buttons）vs Type B（style-3-6 + layout-2，`<sticky-add-cart>` 元素+product-item，无价格/区域提示，显隐行为也不同）。本轮：① style-1 吸底栏全 spec 对齐（标题 20/18 常规、总价 16、划线价 12+#A3A3A3、发货 14+#7A7A7A、税费 12、区域提示条间距/圆角/色、各间距对齐阶梯）；② **数量选择器按 Figma 重建**（灰底 #EBEBEB + #A3A3A3 描边 + 5px 圆角 + 10px 细线加减号 + 高度随按钮 + 数字主区16/吸底14）+ **加购按钮 MD（16/内距24/常规400/h-50）**——改共享 `buy-buttons.liquid` + `icon-minus/plus.liquid`，**影响全站商品页主区 + 各 style 吸底 + quickview + 视频购买弹窗**；③ **划线价 80%→固定 12px** 为全局（`theme.css .price-item--compare`），影响 audience1+2 吸底栏总价划线价（产品卡/集合页用 `compare-price`，不受影响）；④ 主区数量选择器尺寸/字号、主区按钮字号覆盖在 `theme.css`，区域提示条样式在 `product-card.css`。**抽片段时 render 隔离作用域，显式传 product/section/enable 标志/skeleton/region_btn_notification**；吸底价格首屏改走 `price-format`（与 JS `Shopify.formatMoney` 等价、消首屏跳变）。**style-2~6 吸底栏未接共享片段（待评估 Type A/B 是否统一设计）**。**2026-06-30 主区全 spec 对齐（style-1，验收 ok）**：① 媒体画廊桌面+移动对齐 Figma（576-2407/2526/2680/2690/2765）——缩略图固定 45/40 + gap 8/16、裸 chevron 无底、计数 pill 距边 24/16、compare=`btn-secondary-outline`；`theme.js` thumb swiper `slidesPerView:7.5→'auto'` + 移动 `spaceBetween:16`/桌面 8；② 价格 `price.liquid` fs 按场景类 + 划线价 `text-tertiary`；③ 折叠区 16px 黑标题（`fs-body-lg`）+ 行距 16 + 首末行 `mar-t/b-10`（**用工具类避开 `mb-custom`/`pb-custom` 的无单位 `calc(*1px)` 陷阱**——传 `var(--space-N)` 会算成 `calc(16px*1px)` 非法归零）；④ delivery-return 重做为 `bg-soft` 双列网格卡（需补 `gap` 类，`grid-cols` 只设 `--gap` 不写 `gap`）+ 整组 `mar-y-10`；⑤ in-the-box 网格 gap 生效 + box-title `text-secondary`；⑥ pickup `mar-y-4` + 用 `external_affiliate==blank` 包裹整块（消空白间距 + 省脚本）；⑦ region-tip / buy-buttons 间距 16 + `gap-sp-2`；⑧ 配送估算 PDP `bg-soft` 无图标（+ schema `show_estimator_icon` 开关，购物车保留图标白底）；⑨ Core Features `fs-body-lg` + bullet point 内容 `text-secondary`；⑩ 第三方插件 Affirm/Klarna/Selleasy CSS 全 spec（`base-more-style.liquid`，**不改插件生成文件**）；⑪ 设备对比弹窗 `product-compare-popup.css/-modal.liquid` 全 spec——**该 CSS 异步加载（`media=print onload`），字号/颜色用 `fs-*`/`text-*` 类挂 HTML 避 FOUC**，动态内容标签（value `t/p`）无法加类、留 token；⑫ **主区两栏 `<992` 堆叠**（原 768；对齐 container + `.gap` 均在 992 切换的事实；`.main-product` scope，768–991 关 sticky）+ 堆叠间距 30→40 |
| multi-content | （legacy 非 WW 版）| 104 | 待办（中-复杂）；**重建路径已验证**（notepin-s-product ×2、note-0911 ×1 已 1:1 重建为 WW）| — | 跟 ww-multi-content 不同源；硬限制：字号单值无 `_m`、区头无移动端对齐、标题走 fs-medium 流式 → 需 spec PC/MB 时按 §4.12 重建为 WW（保留 section key、改 type、复制内容/图/配色，套 WW 离散字号），不改 legacy 模块（波及 104 实例）|
| tt-fade-switch-video | Fade Switch Video TT | 44 | ✅ index 首迁；scheme 四件套 + 字号/字重/色/间距全 spec + 移除 2 个字号字段 | — | tab 标签/视频标题 fs-headline、描述 fs-body-lg+secondary；tab 栏 pad-4/gap-sp-6/radius-base/bg-soft；新增 Tab Bar Background 色板（visible_if）；超 3 tab 选中回退色仍红待评估；2026-06-15 item 标题/描述/免责声明改为跟随区头对齐 |
| delta-trust-carousel | Delta Trust Carousel | 42 | ✅ product.note-0911 首迁；2026-06-15 按 figma node 19-21 二次对齐（认证名→title-2、描述→body-lg 黑、图标 94、副标题下距 24、轮播 PC/平板 16 / MB 8）+ 新增移动端区头对齐配置 | — | CSS 全内联；卡片背景走独立色板；轮播间距用 `data-custom-breakpoints`（含 0 档），不用运行时 JS 改 spaceBetween；进度条交还统一控件 |
| tt-floating-image | Floating Image TT | 3（note-product2510 / notepin-product2510 / notepin-s-product）| ✅ 迁移完成：**scheme 常开（intrinsic，不加 enable 开关）+ use-color-scheme**；正文 #7A7A7A、区块间距 space-14(56)、视频 radius-lg、移动端轮播 24、断点 767.98；richtext 加「文本样式」(正文/免责声明) + `.richtext-container` 统一 H1–H6 字号 + 区头对齐 PC/MB 配置 | — | 视频浮动边距(m_top/m_bottom)是刻意浮动效果、非 spec；移动端图片轮播 pill(50vw)+75% 露边是设计；图片 PC/移动显隐逻辑仍在 async SCSS（预存 FOUC，待优化）；3 模板移动端已存「左对齐」 |
| tt-category-tab | Category Tab TT | 43 | ✅ 迁移完成：标题锁 fs-title-3、普通标签 fs-headline、按钮标签 fs-body-lg；删 heading_color / heading_size / normal_f_size 3 个字段；按钮默认色 #00D0FF→#8F53ED（hover #07AFD5→#7B35EB）；移除 shadow-sm；断点 768→767.98 | — | 吸顶 nav（sticky）；标题色走 `--color-label-primary`、选中/悬停默认 `--color-highlight-purple`（模块 CSS 在 sectionsTT.min.css，本 PR 只改 .liquid）；3 节日页（xmas×2 / bfcm）把 `.tab-item:hover/.active` 高亮色从模板 html_custom 收回 `tab_active_f_color` 字段（色值不变）；education 页存值同步改紫 |
| testimonials | Testimonials | 8 | 待办 | — | plaud-team 实例 disabled |
| ss-marquee | Marquee SS | 37 | ✅ index 第 2 迁；scheme 四件套 + logo 间距 space-4 + 详情卡 token 化 | — | **CSS 全内联**（common `marquee/style.css` import 已注释=死文件）；详情卡仅 page.education / page.note-product 启用；详情卡用 body-lg（长文阅读，非卡片辅助故用 lg 非 md）；`line-clamp:3` + 宽/高 vw/px 布局值为**经确认的设计特例**（`line-clamp` 是对 vendor §8.3「不截断描述」、vw/px 是对全局「禁写死宽高」的受控例外，均已 PM 确认）；链接 highlight-cyan |
| custom-kol-list | Custom Kol List | 13 | ✅ index 第 3 迁；scheme 四件套 + 区头对齐 + 字号/色/圆角/间距全 spec | — | CSS 在 common（异步 sectionsSS），字号/色/圆角走 critical 工具类挂 HTML 避 FOUC；轮播控件换 `section-swiper`（删旧 trust-swiper 的 50px padding），并按"数量>断点每屏数"用 critical 内联 CSS 控制控件显隐（mobile>1/tablet>2/desktop>每行）；视频弹窗重复 id 是预存问题（多实例同页仅首个可用）；缩略图/头像源图限制已放宽；2026-06-15 按 figma 二次对齐（KOL 名→title-3、头像 56/48、间距 24、移动端区头改左） |
| custom-html | Custom HTML | 18 (17 模板 + footer group) | 🟢 2026-06-30 接入配色方案（验收 ok）：wrapper 补 `use-color-scheme`（已挂 `color-{scheme}`、scheme 常开 → 常开重绑，约定 #2 例外）；footer 隐私提示存值对齐 spec（11px→fs-body-sm、上50/下30→pad-y-10、#666→text-secondary、死类 border-bottom→`.separator-b`）| — | **真实影响极小**：17 个模板实例 `color_scheme:""`（继承 :root=default 方案，其默认色已=spec），且内容都用写死 hex / 内联色 / 非 spec 颜色类 → use-color-scheme 重绑无作用对象、零视觉变化；唯一显现的是 footer 那条分隔线（#EBEBEB）。新增 `.separator-*` 工具类供分隔线复用（详见 §4.1，**需 build**）|
| header + mega-menu | 顶部导航栏 / [New] Tabs Atlassian Mega / [New] Complex Menu Mega / 移动端菜单 | 全站 chrome | ✅ 2026-06-30 验收 + 入日志。Header（logo 88 + 上下 16、去 Go-to-Web 绿 hover、≤1280 顶栏 pad 16、右侧图标 24、汉堡图标、移动账户链接）；**两个桌面大菜单**全 spec + Figma 对齐（tab/面板/promotion 卡/列/链接/右侧三视觉；新增"每行列数 3-5"「视觉列宽 0-100%」配置；视觉区 5px 圆角）；移动菜单展开（间距阶梯 40/24/8、详情/评价/promo 卡、空区块条件渲染）；移动端语言货币地区块；IP 引导条；地区弹窗；badge 默认色 白/红→红/浅红 | menu_atlassian 新增 `products_columns`(3-5,默认3)；menu_simple 新增 `visual_col_width`(0-100,默认50) | 主要在 `menu-link-loop.liquid` / `mm-submenu-list.liquid` / `mm-horizontal-menu.liquid` / `custom-nav.css` / `navigation.css` / `critical-style.liquid` / `header.liquid`；踩坑沉淀见 §4.15–4.20（token→class 全实例、grid-cols gap、my-0、空 wrapper gap、card_bg 菜单信号、生成文件） |
| 全站商品卡（product-item 统一项） | 商品卡（集合/搜索/推荐/菜单/手动网格通用） | 全站 | ✅ 2026-06-30 验收 + 入日志。信息区纵向居中 gap8（挂 HTML，删 2 个 section 重复覆盖）；名称字号；角标内距 4/8 贴角；加购按钮去 140 限宽→全宽；加购区空则不显示；促销关键点小标签统一 14/#7A7A7A/行8列4/margin0（字号色移到容器类、span 只留 chip 盒）；图片区内距 24/16 限"仅菜单"（`card_bg=='white'`）；Manual Grid 卡片按钮去品牌深色主题 | — | `product-item.liquid` / `base-more-style.liquid` / `product-badges.liquid` / `color-swatches.liquid` / `product-grid-manual.liquid`；删了多条 section 级冗余覆盖，统一走全局单一来源 |
