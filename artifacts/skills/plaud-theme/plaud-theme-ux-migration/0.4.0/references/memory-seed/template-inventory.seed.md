# 模板清单（per-template metadata）

> ⚠️ **一次性种子 — 2026-07 快照。** 首次使用时复制到项目 `memory/模板清单.md`，之后即由项目维护，**不再随包更新**。
> 字段含义见 `../project-state-schema.md`；使用规则见 `README.md`。
>
> 🔴 **本文件内的字号 / 间距 / 颜色 / 断点等数值是「实测历史证据」（2026-07 当时仓库的实际状态），不是规范值。规范一律以 `plaud-theme-shared/references/` 为准；两者不一致时以 shared 为准，禁止拿本文件的数值当 spec 落地或反推覆盖 shared。**

> 每完成一个模板加一行。启动新模板审计前先看这张表。

| 模板 | 状态 | section 渲染顺序 | 已迁移模块（在此模板内）| 实例特殊约束 / 历史遗留 |
| --- | --- | --- | --- | --- |
| page.plaud-team-page.json | ✅ DONE | Slideshow → SA: Text → Intelligent Power SS → Delta Accordion ×2 → Testimonials(disabled) → Multi WW ×4(第 4 disabled) → Editorial → custom_html → Landing → FAQ | 8 个：Slideshow / SA: Text / Intelligent Power SS / Delta Accordion / Multi WW / Editorial / Landing / FAQ | Slideshow Hero 用后台 HTML 自定义；Landing Step 2 价格自定档；Multi WW 实例 1 卡片块 `color_scheme: "scheme-6"` 是脏值（schemes 表无此项，走 fallback）；Custom HTML 运营手填不审；2026-06-15 FAQ 按 figma 二次对齐 + 本页 FAQ 移动端区头改居中 |
| product.note-0911 | 🟡 进行中（Multi WW ×6 + Delta Trust Carousel 已迁）| Breadcrumb → Main Product → Category Tab → Multi WW ×2 → Fade Switch Video → Delta Accordion → 旧版 Multi Content → Delta Accordion ×4 → Multi WW ×2 → SA Subscription Compare → Multi WW → Delta Trust Carousel → Marquee → Multi WW → FAQ List → Products Deal → Rich Text → Custom Liquid（disabled：product-set / related-products / 多 faq_item / icon_box）| Multi Content - WW ×6（5 迁 1 跳）、旧版 Multi Content ×1 重建为 WW、Delta Trust Carousel | 6 个 WW 实例：纯图实例（G4VRLD）跳过；2 个图区头移动端左对齐；叠图 banner（ar7fJC「Smarter notes」）副标 title-2/400/渐变挂结构类、主标离散 40/32、描述 20/18；数据价值区（yNzzEn）按 0426pins 重建但**保留 PC 左图右文**（参考页为文左图右）；规格表（99AVzY）20/18 + gap 24/16 + PC 居中。**两处模块级**：①overlay 标题/描述去流式恒用 fs-custom（约 16 实例回归，见 §4.12.1）②底部按钮 mar-t-8 + btn-primary-md + 默认色 #413D3B（3 实例 + 新建默认）。对比图 `image` block 保留未转。**旧版 Multi Content「Dual mode recording」按 §4.12 1:1 重建为 WW**（2 叠图卡：标题 title-3 24/20、描述 body-lg 16/16、内边距 24、卡间距 24/16、区头 40/32+MB左，保留原配色 UUID / 文案 / 左右布局；保留 section key、改 type）。**已迁模块补本页移动端区头左对齐**：Fade Switch Video / FAQ List / Delta Trust Carousel / Marquee（逐实例存值，缺字段走默认居中需补；左值 WW/fade=`left`、delta-trust/marquee=`start`）；Delta Accordion ×5 本就已左。**+ 吸底加购栏 spec 对齐（详见 main-product 模块）** |
| product.notepin-0915 | 🟡 进行中（Multi WW ×5 已迁）| Breadcrumb → Main Product → Category Tab → Multi WW ×2 → Fade Switch Video → Delta Accordion ×4 → SS Text Switch → SA Subscription Compare → Multi WW → Delta Media With Text → Delta Trust Carousel → Marquee → Multi WW → FAQ List → Product Set → Products Deal → Apps → Rich Text（disabled：related-products）| Multi Content - WW ×5（4 迁 1 跳）| 5 个 WW：纯图 LUVPaV 跳过；8azMmV / hmR9qf 区头移动端左；hmR9qf 安全提示移入 Disclaimer；4inYNi（3 列网格）说明 body-lg 16/16 + 区头左 + 列距 24/16 + 脚注移入 Disclaimer；r9cDX3 规格表 20/18 + gap 24/16 + 区头 PC居中/MB左。**纯本页存值、无模块代码改动**（模块此前已迁）。image block 保留未转。已迁模块移动端区头左对齐**已补**：Fade Switch(`left`) / FAQ List(`start`) / Delta Trust(`start`) / Marquee(`start`)；Delta Accordion ×4 本就已左；Category Tab / Delta Media / Rich Text 区头空免。**SS Text Switch 单对齐字段（不分 PC/MB）无法仅左移动端 → 待评估**。**+ 吸底加购栏 spec 对齐（详见 main-product 模块）** |
| product.plaud-note-pro | 🟡 进行中（Multi WW ×4 启用已迁）| Breadcrumb → Main Product → Category Tab → Multi WW ×2 → Fade Switch Video → Delta Accordion ×6 → Delta Rolling Num → Delta Media With Text → SA Subscription Compare → Multi WW(数据价值) → Delta Trust Carousel → Multi WW(规格表) → Related Products → FAQ List → Rich Text → Apps（disabled：Multi WW ×3）| Multi Content - WW ×4 启用（azEQGy 纯图跳过 / ELBerj / 数据价值区 / 规格表）| 纯本页存值、无模块改动。ELBerj 区头移动端左；**数据价值区 AUz7nA = 2 列简单结构（非 yNzzEn 4 列），PC 左文右图，按 Figma 重建为拆分列做 M 端「标题→图→数据→脚注」重排**（大数字 fs-title-1、说明 #7A7A7A、脚注 tertiary、文图间距 48/16）；规格表 20/18+gap24/16+区头PC居中/MB左。3 个 disabled WW 不计。已迁模块移动端区头左对齐**已补**：Fade Switch(`left`)/FAQ List(`start`)/Delta Trust(`start`)；Delta Accordion ×6 本就已左；Category Tab/Delta Media/Rich Text 区头空免。**Delta Rolling Num 模块无任何对齐字段（区头硬走默认居中）→ 需模块代码改动加 header_align 配置（3 实例 blast）→ 待评估**。**+ 吸底加购栏 spec 对齐（详见 main-product 模块）** |
| product.0426pins | 🟡 进行中（本轮 Multi WW ×3 已迁）| Breadcrumb → Main Product → Category Tab → Productivity Card → Delta Accordion ×2 → Multi WW(对比图) → SA Subscription Compare → Multi WW(数据价值) → Delta Media With Text → Delta Trust Carousel → Multi WW(规格表) → FAQ List → Product Set → Rich Text → Reviews → Products Deal（disabled：Delta Accordion ×3 / Multi WW ×1 / Custom Kol List / Brand Logo / Related Products + main 内多 block + 1 个 FAQ item）| Multi Content - WW ×3 实例（对比图区头移动端左对齐 / 数据价值区按 Figma + 规范 / 规格表）| ② 数据价值区按 Figma（PC 对 Figma、M 端规范缩小）：大数字 50/30→32/28 去粗、说明文字 #7A7A7A、脚注 #A3A3A3、移动端重排「标题→图片→数据→脚注」（**新增一个 hide_pc 标题列**排到图片前）、PC 数据列距 32 / 内容↔图 48；**脚注灰必须用内联色**——richtext 字段（rich_description）禁 class/style，会被 Shopify 拒上传，已挪到 textarea 字段（description）+ `<span class>`；模块级修正移动端竖向堆叠行间距（去掉"横向>30 锁 30"逻辑，影响本页 + plaud-team-page 共 3 实例，PC 不变）；Productivity Card SS / Delta Media With Text 审计确认代码已是 spec、未改；Delta Trust Carousel + Faq List TT 本页移动端区头改左对齐（仅存值，Left 值 = `start`）；main-product 主区其余 block 及分给他人的模块未动。**+ 吸底加购栏 spec 对齐（详见 main-product 模块）** |
| index | 🟡 进行中 | Slideshow → Manual Product Grid → Fade Switch Video TT → Multi WW → Marquee → Multi WW → SA Why Trust → Custom Kol List → Sale Reviews → Blog Posts（disabled：第2 Slideshow / Card SS / 第3 Multi WW / Delta Trust Carousel / Custom HTML）| Slideshow（配置层）/ Fade Switch Video TT / Marquee SS / Custom Kol List / Multi WW（Plaud Intelligence + Made for real work 可展开卡片）| SA Why Trust / Sale Reviews / Blog Posts / Manual Product Grid 已分给其他同事；Multi WW 已迁（Plaud Intelligence 配置层 + Made for real work 可展开卡片 2026-06-11 代码层全 spec 对齐）；Slideshow 走配置层(本模板 Image→New Slide 已切)；index 本人可做部分已完成；2026-06-15 各模块移动端标题统一改左对齐（含 Marquee 去内联居中、Delta 新增对齐配置；Sale Reviews/Blog Posts 无配置开关未处理）；Custom Kol List 按 figma 二次对齐 |
| page.notepin-s-product | 🟡 进行中 | Slideshow → 待 audit（其余模块）| Floating Image TT / Slideshow（配置层）/ Multi Content ×2（重建为 WW）| Floating Image TT 已迁（移动端区头/正文存「左对齐」）；Slideshow 走配置层——本页无预置 new_slide，手动新建并切换、开 Enable New Banner、Laptop 宽度 40（见 §4.8.1/4.8.2）；2 个旧版 Multi Content（Amplify 6 职业卡 + Originality 单图）**1:1 重建为 Multi Content - WW**（每卡一个 col_parent、单图用 image_with_text_below，见 §4.12）；旧版 multi-content 临时代码改动已还原；其余模块待 audit |
| page.note-product2510 | 🟡 进行中 | 待 audit（其余模块）| Floating Image TT | 同上（页面 = /pages/plaud-note-ai-notetaking-device）；⚠️ dev 预览此页 floating-image 只渲染 3 block（本地模板 5 个，疑似线上挂载的模板版本与本地不一致，待查，非本次改动）|
| page.notepin-product2510 | 🟡 进行中 | 待 audit（其余模块）| Floating Image TT | 同上（页面 = /pages/plaud-notepin-wearable-ai-note-taker）|
| announcement-newbar + header | 待办 | section group | — | — |
| page.delta-presale | 待办 | 待 audit | — | — |
