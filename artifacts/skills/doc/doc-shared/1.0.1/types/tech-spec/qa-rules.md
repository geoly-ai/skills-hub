# 质检规则（技术 Spec）

对齐 design.md §5.4。引擎通用规则（T1–T12、L1/L2、R1、C1、图字号下限）不在此重复列出。

| 编号 | 规则 | 判据 | 定级 | 自动/人工 |
|---|---|---|---|---|
| SPEC-01 | 必备章节齐全 | skeleton.md 中 required=是 的 9 节都能匹配到标题或锚点（文档控制与修订记录由引擎按 doc.json 生成、摘要由 summary 块生成，不计入正文必备章节） | 必改 | 自动（引擎 S1） |
| SPEC-02 | 「备选方案」节至少 2 个方案 | 表格行数 ≥ 2，或三级标题数 ≥ 2（ALT-1、ALT-2…） | 提示（试运行；原定 必改） | 自动（qa_rules.py） |
| SPEC-03 | 至少一张架构图与一张时序或状态图；每张图有图号、被引用 | figures 目录含 {#fig: 锚点且被 @fig: 引用；图类型按文件名或 front-matter 判断至少含 arch 与 seq/state 各一 | 提示（试运行；原定 必改） | 自动（qa_rules.py） |
| SPEC-04 | 每张图后有文字等价说明 | @fig:x 引用所在段落非空且字数 > 20（只查存在，不判说明质量） | 提示（试运行；原定 建议） | 自动（qa_rules.py） |
| SPEC-05 | 回滚节非空且含判据关键词 | 「迁移、上线与回滚」节含"回滚条件""触发""判据"任一关键词 | 提示（试运行；原定 必改） | 自动（qa_rules.py） |
| SPEC-06 | 代码块标注语言 | 围栏代码块起始的三个反引号后紧跟语言标识，且语言在 pack.draft.json 的 allowed_languages 内 | 提示（试运行；原定 建议） | 自动（未标语言由引擎 L8 建议；语言白名单 qa_rules.py 常量 ALLOWED_LANGS） |
| SPEC-07 | 接口表含错误码列 | 若存在「接口设计」节的表格，须含"错误码"或"error_code"列 | 提示（试运行；原定 必改（该节存在时）） | 自动（qa_rules.py） |
| SPEC-08 | 版本、环境信息出现在代码示例附近 | 代码块前后 3 行内出现版本号或环境标注 | 提示（试运行；原定 提示） | 自动（qa_rules.py） |
| SPEC-09 | 事实 / 推断 / 未知标注 | 正文出现"[待确认]""[推断]""[未知]"等标注关键词至少一处（存在性检查，衡量是否做了区分，不保证覆盖完整） | 提示（试运行；原定 提示） | 自动（qa_rules.py） |
| SPEC-H1 | 备选方案是真实可行方案，不是稻草人 | — | — | 人工 |
| SPEC-H2 | 取舍口径一致 | — | — | 人工 |
| SPEC-H3 | 失败模式覆盖关键依赖 | — | — | 人工 |
| SPEC-H4 | 回滚真的可执行 | — | — | 人工 |
| SPEC-H5 | 安全与隐私是否按实际风险纳入 | — | — | 人工 |
| SPEC-H6 | 结论与证据分开 | — | — | 人工 |

## 人工检查清单

- [ ] 备选方案用同一组维度（成本、性能、迁移难度、风险）比较，不是各说各话
- [ ] 否决理由具体，不是"更复杂"这类空泛判断
- [ ] 架构图真实反映方案设计，不是营销风格的示意图
- [ ] 组件职责边界清楚（负责什么、不负责什么）
- [ ] 迁移步骤可执行，开关配置项具体
- [ ] 未决问题不包含应在评审阶段关闭的"要不要做"类问题

## 伪问题（不要报）

- 「现状架构」「接口设计」两节缺失（按需章节，纯新建系统或无对外接口时可以不写，但要在正文写明不适用理由）。
- 备选方案节里方案数量恰好 2 个（满足下限，不是"应该更多"）。

## 实现说明（W3-A，2026-09-15）

- 自动规则由同目录 qa_rules.py 实现，新规则一律定级「提示」试运行（doc-qa/SKILL.md「类型包接入」），「原定」为本表原定级；样张与真实文档校准后用 pack.json qa.severity_overrides 升级。校准结果见 ~/workspace/docs/doc-skill-platform-2026-09-15/wave2/qa-calibration.md §6。
- 与引擎重复的判据不在 qa_rules.py 重复实现，「自动/人工」列写明由哪条引擎规则执行（S1 必备章节、S3 编号重复与跳号、L8 代码块语言、R1 绝对化用语、T11 验收列空泛词、X2 图表未引用）。
- 跨文档规则用 ctx.related(type, role=…)：按 role 精确命中且可读才用；读不出、或同类型未写 role 的条目多于 1 条时按关联文档缺失处理（pack.json related 的 for_gate 为真时必改）；同类型未写 role 的条目恰好 1 条时兼容读取并提示补 role（迁移期回退，契约定 role 必填后删除）。
- 数据块（CSV）各行共用指令行号，逐行问题报全文级（行号 0），摘录写数据文件与 CSV 行号，避免被「同规则同行只留一条」合并。
- 关键数字（X4）与高亮限额（H1–H3）口径在 pack.json qa.key_figures、qa.highlight_limits 声明；key_figures 定级一律提示试运行，X4 只查多处取值是否一致，不查数字是否存在或正确。
