# 写作约束快照（技术 Spec / Design RFC）

来源：lark-doc/references/genres/technical-doc.md（体裁 workplace.technical_doc，主模式 design_rfc）
读取日期：2026-09-15。

## 体裁规则表（硬约束，原文摘录）

| 规则项 | 规则 |
|---|---|
| 写作风格 | 精确、可证伪、术语与版本稳定；规范词仅用于明确采用的互操作、安全或验收语义 |
| 视觉约束 | 在有明确内容作用时用代码、表格、架构 / 状态 / 时序图等组件降低实现与诊断成本，但不得替代契约、证据或操作说明 |
| 内容逻辑 | 必须且只能选 design_rfc、api_reference、incident_diagnostic 一种主模式（本包锁定 design_rfc；api_reference 见 api-reference 类型包） |
| 事实 / 边界 | 标对象、环境、版本、时间、范围和证据窗；事实、推断、决定、未知分开；示例 / 图不替代契约；任何改状态动作须有授权、影响、停止、还原和恢复验证 |
| 错误 | 按关键词路由、三模式混写、设计无取舍 / 验收、未知故障直接定根因、改状态无授权 / 停止 / 还原或图作唯一证据，任一出现即失败 |

## 主模式选择（原文摘录）

design_rfc：读者任务是"评审者能批准并实现未来技术状态，理解替代、后果和验收"。排除：产品可观察行为走 PRD；既定路径走 SOP。本包只承载 design_rfc；incident_diagnostic（事故诊断）与本包无关，不适用。

## 共同证据边界（原文摘录）

标明对象、环境、版本、时间、范围 / 前置和证据位置 / 窗口；结论回链仓库、IDL / schema、日志、metrics、traces、变更记录或验证实验。缺口就近使用具体占位、收窄或 blocked。代码与命令示例须实际验证并标环境 / 版本；架构、状态或时序图必须附文字等价，不能成为唯一证据或唯一操作说明。

## Design RFC 结构（原文摘录）

按问题证据 → 目标 / 非目标 → 约束 / 不变量 → 真实备选与同口径取舍 → 接口 / 数据 / 状态设计 → 失败、安全、兼容与迁移 → 上线 / rollback → 可观测性、测试 / 验收 → 未决决定推进。每项关键决定写 why、被否方案及后果；不得隐藏低置信度或版本偏差。

## 本包在此基础上的补充（不在原文中，是本平台自定）

- 代码块政策：decisions.md ⑦A——允许块级代码块并标语言，行内代码仍全局禁止（T9）。
- 接口设计节与 api-reference 类型包的边界：本节只覆盖本方案新增或变更的核心接口摘要；完整契约文档（版本、权限、错误码全集、限流分页）属于 api-reference，本节可用 [链接](../api-reference/...) 交叉引用。
- 备选方案至少 2 个为本包质检新增（design.md §5.4），原文只要求"真实备选"未规定数量下限。
