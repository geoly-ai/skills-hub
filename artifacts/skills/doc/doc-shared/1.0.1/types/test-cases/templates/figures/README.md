# 测试用例图源说明

测试用例类型包以数据表为正文主体，不需要图。本目录保留是为了与其他类型包的目录结构一致，当前为空（无 .mmd / .fig.json 模板文件）。

若某个测试用例文档需要展示流程覆盖概览（如状态覆盖图），应参照 test-plan 包的 coverage-heatmap.fig.json 或 tech-spec 包的 state-machine.mmd 约定新增，不在本包默认模板中提供。
