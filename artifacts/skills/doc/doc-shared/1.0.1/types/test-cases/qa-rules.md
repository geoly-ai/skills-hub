# 质检规则（测试用例）

对齐 design.md §5.5。引擎通用规则（T1–T12、L1/L2、R1、C1、宽表 landscape 处理）不在此重复列出。

| 编号 | 规则 | 判据 | 定级 | 自动/人工 |
|---|---|---|---|---|
| CASE-01 | 必填字段非空；编号唯一 | data/cases.csv 的用例编号、标题、关联需求、优先级、类型、前置条件、测试步骤、预期结果 列逐行非空；用例编号不重复 | 提示（试运行；原定 必改） | 自动（qa_rules.py，全量 CSV；正文渲染行的编号重复另由引擎 S3 必改） |
| CASE-02 | 关联需求编号在 PRD 中存在 | 每条用例的关联需求（可多个）都能在关联 PRD 的 data/requirements.csv 中找到 | 提示（试运行；原定 必改） | 自动（qa_rules.py，ctx.related 跨读 PRD） |
| CASE-03 | PRD 每条 P0/P1 需求至少一条用例（D2 门） | 关联 PRD 中 P0/P1 的 REQ 编号，在本包 data/cases.csv 的关联需求列中至少出现一次 | 提示（试运行；原定 必改（D2 门条件）） | 自动（qa_rules.py，ctx.related 跨读 PRD） |
| CASE-04 | 预期结果无空泛词 | 预期结果列不单独出现"正常""成功"（不带任何限定词的孤立用法） | 提示 | 自动（引擎 T11，pack.json banned_terms 与内置同片段时类型包定级与文案优先） |
| CASE-05 | 步骤数与预期结果数对应 | 预期结果有编号且条数与测试步骤编号条数不同（含步骤未编号）；预期结果不编号视为整体结果，不查 | 提示（试运行；原定 建议） | 自动（qa_rules.py） |
| CASE-06 | 异常类用例占比低于阈值时提示 | 类型为"异常"的用例数 / 总用例数，四舍五入到整数百分比后不在 20%–40%（qa_rules.py 常量 ABNORMAL_RANGE） | 提示 | 自动（qa_rules.py） |
| CASE-07 | 优先级取值合法 | 优先级列取值 ∈ {P0, P1, P2, P3} | 提示（试运行；原定 必改） | 自动（qa_rules.py） |
| CASE-08 | 类型取值合法 | 类型列取值 ∈ {功能, 接口, 兼容, 性能, 安全, 异常, 边界} | 提示（试运行；原定 必改） | 自动（qa_rules.py） |
| CASE-09 | 模块小节都写了 | data/cases.csv 每个模块（用例编号 TC-<模块>- 前缀）的用例都出现在正文（数据块渲染的编号实体）；缺哪个模块报哪个 | 提示（试运行） | 自动（qa_rules.py） |
| CASE-H1 | 步骤可复现、无跳步 | — | — | 人工 |
| CASE-H2 | 用例覆盖边界值、异常、并发这类高风险点 | — | — | 人工 |
| CASE-H3 | 预期结果与需求验收标准一致 | — | — | 人工 |
| CASE-H4 | 优先级分布合理 | — | — | 人工 |
| CASE-H5 | 测试数据不含真实隐私数据 | — | — | 人工 |

## 人工检查清单

- [ ] 抽样核对至少 5 条用例的步骤，确认不了解背景的执行者能直接照做
- [ ] 高风险需求（并发、金额计算、权限）是否都有对应的异常或边界用例
- [ ] 测试数据全部为虚构标识符，无真实姓名、手机号、身份证等
- [ ] 关联需求列的多需求标注（分号分隔）语义清楚，不是随意堆砌

## 伪问题（不要报）

- 部分 P2/P3 优先级用例的"自动化"列为"否"（低优先级用例暂不自动化是合理的资源取舍，不算漏项）。
- 某条用例关联多个 REQ 编号（合理，见 skeleton.md 关联需求写法要点）。

## 实现说明（W3-A，2026-09-15）

- 自动规则由同目录 qa_rules.py 实现，新规则一律定级「提示」试运行（doc-qa/SKILL.md「类型包接入」），「原定」为本表原定级；样张与真实文档校准后用 pack.json qa.severity_overrides 升级。校准结果见 ~/workspace/docs/doc-skill-platform-2026-09-15/wave2/qa-calibration.md §6。
- 与引擎重复的判据不在 qa_rules.py 重复实现，「自动/人工」列写明由哪条引擎规则执行（S1 必备章节、S3 编号重复与跳号、L8 代码块语言、R1 绝对化用语、T11 验收列空泛词、X2 图表未引用）。
- 跨文档规则用 ctx.related(type, role=…)：按 role 精确命中且可读才用；读不出、或同类型未写 role 的条目多于 1 条时按关联文档缺失处理（pack.json related 的 for_gate 为真时必改）；同类型未写 role 的条目恰好 1 条时兼容读取并提示补 role（迁移期回退，契约定 role 必填后删除）。
- 数据块（CSV）各行共用指令行号，逐行问题报全文级（行号 0），摘录写数据文件与 CSV 行号，避免被「同规则同行只留一条」合并。
- 关键数字（X4）与高亮限额（H1–H3）口径在 pack.json qa.key_figures、qa.highlight_limits 声明；key_figures 定级一律提示试运行，X4 只查多处取值是否一致，不查数字是否存在或正确。
