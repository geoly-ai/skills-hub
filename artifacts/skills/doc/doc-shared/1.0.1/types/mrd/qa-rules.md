# 质检规则（MRD）

对齐 design.md §5.3。引擎通用规则（T1–T12、L1/L2、R1、C1）不在此重复列出。

| 编号 | 规则 | 判据 | 定级 | 自动/人工 |
|---|---|---|---|---|
| MRD-01 | 每个市场数字所在段落或表格有来源与年份 | 「市场概况」「目标用户与细分」「竞争分析」三节与摘要块里含 %、亿、万 的段落需有来源标注（来源、依据、统计、报告、访谈、埋点、工单、实测等或脚注）；表格有来源 / 依据 / 出处列时该行此列非空，没有此列时该行需括注来源（只查存在，不判来源真实性） | 提示（试运行；原定 建议） | 自动（qa_rules.py） |
| MRD-02 | 竞品矩阵有依据列或脚注 | 「竞争分析」节的表格含"依据"列或表格下方有脚注 | 提示（试运行；原定 建议） | 自动（qa_rules.py） |
| MRD-03 | 需求 MR 编号唯一、有优先级 | 正则匹配 MR-\d{3}，编号不重复；同表格含优先级列且非空 | 提示（试运行；原定 必改） | 自动（qa_rules.py 查优先级；编号重复由引擎 S3 必改，跳号由 S3 contiguous 提示） |
| MRD-04 | 绝对化与夸大用语 | 复用引擎 R1 规则表，MRD 场景额外覆盖"颠覆""独家""唯一"等市场用语 | 必改 | 自动（引擎 R1 + pack.json banned_terms） |
| MRD-05 | 必备章节齐全 | skeleton.md 中 required=是 的 7 节都能匹配到标题或锚点（摘要由 summary 块生成，不计入正文必备章节） | 必改 | 自动（引擎 S1） |
| MRD-H1 | 细分是否互斥、是否可触达 | — | — | 人工 |
| MRD-H2 | 竞品矩阵每格依据是否成立；竞争分析是否只列优点 | — | — | 人工 |
| MRD-H3 | 结论是否由数据支撑，而非反推 | — | — | 人工 |
| MRD-H4 | 定位是否清晰可区分 | — | — | 人工 |

## 人工检查清单

- [ ] 市场数字的来源标注具体到可复核程度（报告名 + 发布方 + 年份，或内部系统 + 口径），不是"网络数据"这类泛称
- [ ] 竞品选择覆盖直接竞品与替代品，不是只挑弱于自己的对手
- [ ] 差异化机会能从竞品矩阵的空白格推导出来，不是凭空写
- [ ] 用户需求的来源标注区分了"用户证据"与"内部假设"
- [ ] 商业模式假设都配了验证方式与时间点
- [ ] 风险节写清了"假设不成立时结论会怎么变"

## 伪问题（不要报）

- 商业模式与定价假设、进入策略与里程碑两节缺失（按需章节，早期市场评估可以不写）。
- 竞品矩阵里某一维度全部竞品都是 ○（说明市场存在共同空白，属于机会点，不是数据错误）。

## 实现说明（W3-A，2026-09-15）

- 自动规则由同目录 qa_rules.py 实现，新规则一律定级「提示」试运行（doc-qa/SKILL.md「类型包接入」），「原定」为本表原定级；样张与真实文档校准后用 pack.json qa.severity_overrides 升级。校准结果见 ~/workspace/docs/doc-skill-platform-2026-09-15/wave2/qa-calibration.md §6。
- 与引擎重复的判据不在 qa_rules.py 重复实现，「自动/人工」列写明由哪条引擎规则执行（S1 必备章节、S3 编号重复与跳号、L8 代码块语言、R1 绝对化用语、T11 验收列空泛词、X2 图表未引用）。
- 跨文档规则用 ctx.related(type, role=…)：按 role 精确命中且可读才用；读不出、或同类型未写 role 的条目多于 1 条时按关联文档缺失处理（pack.json related 的 for_gate 为真时必改）；同类型未写 role 的条目恰好 1 条时兼容读取并提示补 role（迁移期回退，契约定 role 必填后删除）。
- 数据块（CSV）各行共用指令行号，逐行问题报全文级（行号 0），摘录写数据文件与 CSV 行号，避免被「同规则同行只留一条」合并。
- 关键数字（X4）与高亮限额（H1–H3）口径在 pack.json qa.key_figures、qa.highlight_limits 声明；key_figures 定级一律提示试运行，X4 只查多处取值是否一致，不查数字是否存在或正确。
