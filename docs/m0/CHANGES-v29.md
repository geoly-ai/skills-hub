# M0 v29 变更台账 —— 修一次**我自己造成的回归**

第二十八轮的评审在跑到一半时被中断，但它在中断前已经报出一条，而且**是对的**：

> 现行 §5.10 只保留了隔离对象的身份字段，原先「planned 时按源/隔离位置实况幂等完成隔离」的
> 恢复规则**并未落在现行规格**。

自查属实：v26 / v27 的整块替换，锚点跨过并**吞掉了 §5.10 里的四整节** ——

- 「隔离的范围（四样）」
- 「六步与逐崩溃点」
- 「物理实况」的两张恢复表（三个 rename 项 + `ledger.transaction`）
- 「同事务绑定」

v29 已全部补回，并顺手把 ④ 那一步补成「原子写 `state=isolated` **并同时补入 `cur` / `child_op`**」，
与 v28 的两段式时序对齐。

🔴 **这是同类事故的第二次**（v12 那次吞掉了 rollback 的终结顺序）。
教训记在这里：**用「从 A 节标题替换到 B 节标题」的方式改长文档，会静默删掉中间的一切。**
此后每轮改完都要跑一次小节结构自查，不能只查章节引用是否有效 ——
**被删掉的节不会产生失效引用，它是静默的。**

## 顺带修掉的两处结构问题（同一次自查发现）

| 问题 | 修法 |
|---|---|
| §5.7「交换语义的真实承诺」被排到了 §5.10 **之后** | 移回 §5.6 与 §5.8 之间 |
| §5.4 的标题仍是 v6 的「**四个位置**的存在组合 + 实测摘要」 | v10 早已换成幂等前向恢复，标题同步改为「幂等前向恢复（v10 换掉了 v6–v9 的物理状态穷尽表路线）」 |

## 本轮不含新的规范决定

v29 只做回归修复与结构订正，**没有引入任何新语义**。
v28 的三组改动（`cur`/`target` 的权威来源与两段式时序、零 item child 仍写 manifest、
`child_registered` 的 12 行优先级矩阵）原样保留，等待复审。
