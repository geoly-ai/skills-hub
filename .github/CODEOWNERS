# 06-submission.md §10：CODEOWNERS 覆盖 /.github/、/artifacts/、/registry/、
# /cli/、/scripts/、/docs/。
#
# 🔴 **这份文件是分支保护的一半，另一半在仓库设置里**（见
#    docs/m3/00-branch-protection.md）。CODEOWNERS 自己只决定「谁被请求审查」；
#    「必须有 owner approve 才能合并」是设置里的 Require review from Code Owners。
#    只写这份文件、不开那个开关 = 一道摆设。
#
# ⚠️ **@geoly-ai/maintainers 现在还不存在。**
#    🔴 **不要把这当成 fail-closed**（Codex 2026-08-31 纠正了我的原判断）：
#    GitHub 对无效 owner 的处理是把这份文件标成 **CODEOWNERS 错误**，
#    行为随配置而异 —— 我没有证据说它一定「PR 合不了」。
#    正确做法是**先建团队，再用一张真 PR 或 API 验一次**
#    「owner review 确实被要求了」，别靠推断。见 docs/m3/00-branch-protection.md。

# 兜底：其余路径也要有人看
*                   @geoly-ai/maintainers

# §5 硬拒的那几条路径 —— 投稿 PR 改这里会被 CI 直接拦下，
# 走到人工审这一步的只可能是 org 成员开的 PR
/.github/           @geoly-ai/maintainers
/scripts/           @geoly-ai/maintainers
/cli/               @geoly-ai/maintainers
/docs/              @geoly-ai/maintainers

# 🔴 registry/ 与 artifacts/ 是 registry 的**事实本体**：
#    保留 namespace、维护者名单、owner 表、快照。
#    promotion PR 改的正是这里，它必须走人工审（§3：promote 不是自动发布）。
/registry/          @geoly-ai/maintainers
/artifacts/         @geoly-ai/maintainers

# 🔴 `submissions/**` **也**归 maintainers —— 上面的 `*` 兜底已经覆盖它，
#    这里写出来是为了不让人误以为它是个例外（我原先的注释写反了，
#    Codex 2026-08-31 指出：`*` 覆盖一切，「投稿目录不设 owner」是错的）。
#    §7 说的本来就是「**维护者** approve」，所以要求 maintainer review 是对的。
/submissions/       @geoly-ai/maintainers

# ⚠️ CODEOWNERS 只管**谁来审**，管不了**要几个人审**。
#    §7 的 Tier 0/1 一名、Tier 2 两名是按 capability 决定的，
#    CODEOWNERS 表达不了「按内容决定人数」——那一半在别处，
#    见 docs/m3/00-branch-protection.md 里「Tier 2 的第二名」那一节。
